 {{#let this.foos as |query|}}
    {{log 'should only print once' query}}
  {{/let}}
{{!--
{{#if this.foos.isResolved}}
  <ul>
    {{#each this.foos.value as |foo|}}
      <li>{{foo.id}} - {{foo.name}}</li>
    {{/each}}
  </ul>
{{else}}
  {{this.foos.error}}
{{/if}}
--}}